# Fallback when building with Nixpacks (no Dockerfile): install git via Nix + apt.
# Prefer deploying with Dockerfile on Railway so `git` is always present.
[phases.setup]
nixPkgs = ["git"]
aptPkgs = ["git"]
